---
pagetitle: "Wordbank"
---

```{=html}
<div class="wb-hero">
  <img class="walrus" src="images/walrus.png" alt="Wordbank walrus">
  <div class="wb-hero-title">
    <div class="wb-title">Wordbank</div>
    <p>An open database of children's vocabulary development</p>
  </div>
  <div>
    <a href="https://mb-cdi.stanford.edu/"><img class="mcdi" src="images/mcdi.png" alt="MB-CDI"></a>
    <a href="https://langcog.stanford.edu/"><img class="lab" src="images/lab_logo.png" alt="Language and Cognition Lab"></a>
  </div>
</div>
```

Wordbank archives data from the [MacArthur-Bates Communicative Development
Inventories](https://mb-cdi.stanford.edu/) (CDIs), parent-report instruments
that measure early language development. Data are contributed by researchers
around the world and are freely available for browsing, visualization, and
download.

{{< include _load-instruments.qmd >}}

```{ojs stats-cards}
html`<div class="stats-row">
  <div class="stat-card"><div class="stat-n">${stats.n_administrations.toLocaleString()}</div><div class="stat-label">CDI administrations</div></div>
  <div class="stat-card"><div class="stat-n">${stats.n_children.toLocaleString()}</div><div class="stat-label">children</div></div>
  <div class="stat-card"><div class="stat-n">${stats.n_languages}</div><div class="stat-label">languages</div></div>
  <div class="stat-card"><div class="stat-n">${stats.n_instruments}</div><div class="stat-label">instruments</div></div>
</div>`
```

```{ojs bubble-setup}
//| output: false
d3 = require("d3@7")
langs = transpose(lang_stats)
```

```{ojs language-bubbles}
// the language bubble pack ("snail") from the original wordbank home page:
// one bubble per language, area ~ n_children^0.65, HCL rainbow by size rank
{
  const width = 640, height = 560;

  const root = d3.pack()
    .size([width, height])
    .padding(5)(
      d3.hierarchy({ children: langs })
        .sum((d) => Math.pow(d.count, 0.65))
        .sort((a, b) => b.value - a.value)
    );

  const color = new Map(langs.map((d, i) =>
    [d.name, d3.hcl((i * 360 / langs.length) % 360, 60, 60).formatHex()]));

  const svg = d3.create("svg")
    .attr("viewBox", [0, 0, width, height])
    .attr("style", "max-width: 100%; height: auto; display: block; margin: 0 auto;");

  const node = svg.selectAll("g")
    .data(root.leaves())
    .join("g")
    .attr("transform", (d) => `translate(${d.x},${d.y})`);

  node.append("circle")
    .attr("r", (d) => d.r)
    .attr("fill", (d) => color.get(d.data.name));

  node.append("title")
    .text((d) => `${d.data.language}: ${d.data.count.toLocaleString()} children`);

  // fit label to bubble (same rule as the original: min(2r, scaled-to-width)),
  // measuring text with a canvas so the svg never needs to touch the DOM
  const ctx = document.createElement("canvas").getContext("2d");
  ctx.font = "12px sans-serif";
  const fontSize = (d) => {
    const w = ctx.measureText(d.data.name).width;
    return Math.max(0, Math.min(2 * d.r, (2 * d.r - 8) / w * 12));
  };

  node.append("text")
    .attr("dy", ".35em")
    .attr("text-anchor", "middle")
    .attr("fill", "white")
    .attr("style", "font-family: var(--sans-serif); pointer-events: none;")
    .style("font-size", (d) => fontSize(d) + "px")
    .text((d) => d.data.name);

  return svg.node();
}
```

## Explore

```{=html}
<div class="analysis-thumbs">
  <a href="norms.html">
    <img src="images/sample_plots/vocab_xkcd.png" alt="">
    <h4>Vocabulary Norms</h4>
    <p>Growth curves of vocabulary size, by language and demographic group.</p>
  </a>
  <a href="trajectories.html">
    <img src="images/sample_plots/items_xkcd.png" alt="">
    <h4>Item Trajectories</h4>
    <p>Acquisition trajectories of individual words.</p>
  </a>
  <a href="crossling.html">
    <img src="images/sample_plots/items_xkcd.png" alt="">
    <h4>Cross-Linguistic</h4>
    <p>Compare word meanings across languages.</p>
  </a>
  <a href="data.html">
    <img src="images/sample_plots/vocab_xkcd.png" alt="">
    <h4>Data</h4>
    <p>Browse and download the full dataset on Redivis.</p>
  </a>
</div>
```

## Citation

Frank, M. C., Braginsky, M., Yurovsky, D., & Marchman, V. A. (2017).
Wordbank: An open repository for developmental vocabulary data.
*Journal of Child Language, 44*(3), 677–694.
