# credentials — never commit
.secrets

# ETL output (uploaded to Redivis, not stored in git)
data/

# R
.Rproj.user
.Rhistory
.RData
.Rprofile.local

# Quarto
/.quarto/
_site/
/_freeze/

.DS_Store

# derived slices are build artifacts, except responses/ which is the compact
# committed source CI builds the others from
slices/*
!slices/responses
!slices/networks

**/*.quarto_ipynb
